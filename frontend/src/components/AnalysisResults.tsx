
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Separator } from "@/components/ui/separator";
import { Badge } from "@/components/ui/badge";

const AnalysisResults = ({ analysis }) => {
  if (!analysis) {
    return (
      <Card className="shadow-sm mt-6">
        <CardHeader className="pb-3">
          <CardTitle className="text-lg font-medium">Document Analysis</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="flex flex-col items-center justify-center py-8 text-center">
            <p className="text-muted-foreground">
              Select "Analyze" on a document to view AI-generated insights
            </p>
          </div>
        </CardContent>
      </Card>
    );
  }

  const { summary, key_insights, risk_factors } = analysis || {
    summary: "This is a placeholder summary of the document. The actual summary will be generated by the AI when a document is analyzed.",
    key_insights: [
      "Key insight 1 about financial performance",
      "Key insight 2 about market trends",
      "Key insight 3 about risk exposure"
    ],
    risk_factors: [
      { name: "Market Risk", score: "Medium", details: "Fluctuating interest rates may impact investment returns" },
      { name: "Credit Risk", score: "Low", details: "Well-diversified portfolio with strong counterparties" },
      { name: "Operational Risk", score: "Low", details: "Robust internal controls and procedures in place" }
    ]
  };

  return (
    <Card className="shadow-sm mt-6">
      <CardHeader className="pb-3">
        <CardTitle className="text-lg font-medium">Document Analysis</CardTitle>
      </CardHeader>
      <CardContent className="space-y-6">
        <div>
          <h3 className="font-medium text-sm text-muted-foreground mb-2">Summary</h3>
          <p>{summary}</p>
        </div>
        
        <Separator />
        
        <div>
          <h3 className="font-medium text-sm text-muted-foreground mb-2">Key Insights</h3>
          <ul className="list-disc pl-5 space-y-1">
            {key_insights.map((insight, index) => (
              <li key={index}>{insight}</li>
            ))}
          </ul>
        </div>
        
        <Separator />
        
        <div>
          <h3 className="font-medium text-sm text-muted-foreground mb-2">Risk Assessment</h3>
          <div className="space-y-3">
            {risk_factors.map((risk, index) => (
              <div key={index} className="flex justify-between items-center">
                <div>
                  <p className="font-medium">{risk.name}</p>
                  <p className="text-sm text-muted-foreground">{risk.details}</p>
                </div>
                <Badge 
                  className={
                    risk.score === "High" ? "bg-red-100 text-red-800" :
                    risk.score === "Medium" ? "bg-amber-100 text-amber-800" :
                    "bg-green-100 text-green-800"
                  }
                >
                  {risk.score}
                </Badge>
              </div>
            ))}
          </div>
        </div>
      </CardContent>
    </Card>
  );
};

export default AnalysisResults;
